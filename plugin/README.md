# Plug-in Component API Definitions

The plugin/ directory contains the interface definitions for GNU Radio components
including:

- blocks (block modules)
- scheduler components
- domain components

to fit into the plugin architecture
