module: pdu
block: pdu_to_stream
label: PDU to Stream
blocktype: sync_block

parameters:
-   id: itemsize
    label: Item Size
    dtype: size_t
    settable: false
    default: 0
    grc:
        hide: part
ports:
    - domain: message
      id: pdus
      direction: input
      optional: true

    - domain: stream
      id: out
      direction: output
      type: untyped
      size: parameters/itemsize

implementations:
    - id: cpu
# -   id: cuda

file_format: 1
