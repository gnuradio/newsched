module: blocks
block: copy
label: Copy
blocktype: sync

grc:
    flags: [ python ]
    templates:
        imports: from newsched import blocks
        make: blocks.copy(${type.itemsize})

parameters:
    - id: itemsize
      label: Item Size
      dtype: size_t
      settable: false
      default: 0

ports:
    - domain: stream
      id: in
      direction: input
      type: untyped
      size: itemsize

    - domain: stream
      id: out
      direction: output
      type: untyped
      size: itemsize

implementations:
    - id: cpu
    - id: cuda

file_format: 1
