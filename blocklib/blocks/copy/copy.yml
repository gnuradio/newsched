module: blocks
block: copy
label: Copy
blocktype: sync_block

parameters:
    - id: itemsize
      label: Item Size
      dtype: size_t
      settable: false
      default: 0
      grc:
          hide: all
ports:
    - domain: stream
      id: in
      direction: input
      type: untyped
      size: parameters/itemsize

    - domain: stream
      id: out
      direction: output
      type: untyped
      size: parameters/itemsize

implementations:
    - id: cpu
    - id: cuda

file_format: 1
