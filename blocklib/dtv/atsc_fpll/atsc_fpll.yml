module: dtv
block: atsc_fpll
label: ATSC FPLL

properties:
-   id: blocktype
    value: sync

parameters:
-   id: rate
    label: Rate
    dtype: float 
    settable: false

ports:
-   domain: stream
    id: in
    direction: input
    type: gr_complex

-   domain: stream
    id: out
    direction: output
    type: float

implementations:
-   id: cpu
# -   id: cuda

file_format: 1
